import 'package:flutter/material.dart';

class RoundField extends StatelessWidget {
  const RoundField({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text(
      "Round",
      textAlign: TextAlign.center,
    );
  }
}
